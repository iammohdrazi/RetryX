from setuptools import setup, find_packages

setup(
    name="retryx",
    version="0.1.0",
    description="A simple retry/backoff handler for Python",
    packages=find_packages(),
    python_requires=">=3.7",
)
